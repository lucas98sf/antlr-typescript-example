{
  "name": "compiler-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "bun --filter=editor dev",
    "compile": "bun --filter=compiler compile",
    "generate": "bun --filter=compiler generate",
    "test": "bun --filter=compiler test"
  }
}