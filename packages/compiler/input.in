new a : text;
new b, c, d : number;
write("Hello World");

a = read("enter a string");
write(a);

b = (2 + 3.14) - 5 * 10 + 1 / 100;
c = 8.56 + 1.44;
d = c - 5;
write(d);
is d > 5 ?
   write("> 5"); 
 : write("< 5");
;

new x, y, z : number;
x = read("enter a number");
y = x + 5 + x;
... x < y >
    write("x: ");
    write(x);
    x = x + 1;
;

>
    write("In do-while loop");
    z = read("Enter a number less than 5: ");
... z >= 5;

// should error
// let e : text;
// write(e);